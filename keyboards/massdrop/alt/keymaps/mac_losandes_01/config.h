#define TAPPING_TOGGLE 2    // 2 taps for TT (double-tap to toggle layer)
#define LEADER_TIMEOUT 500  // The time to wait before processing a leader key combo

void open_alfred(void);
void open_spotlight(void);
